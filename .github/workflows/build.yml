name: Build firmware

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    # Pin the reusable workflow so the module and build procedure stay in sync.
    uses: nxtkb/zmk-feature-codex-micro/.github/workflows/build-user-config.yml@077cc618ac3beb9acb92f86ee067d676ed9629f9
    with:
      build_matrix_path: build.yaml
      archive_name: ferris-sweep-firmware

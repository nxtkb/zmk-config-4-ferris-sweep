name: Build firmware

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    # Pin the reusable workflow so the module and build procedure stay in sync.
    uses: nxtkb/zmk-feature-codex-micro/.github/workflows/build-user-config.yml@cb08948c527a206f02b9ca7a567bc2437ccd4970
    with:
      build_matrix_path: build.yaml
      archive_name: ferris-sweep-firmware
